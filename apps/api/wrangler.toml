name = "finhome" # Aligned with CI expected Worker name to avoid duplicate script creation causing queue consumer conflicts
main = "src/index.ts"
compatibility_date = "2023-12-01"
compatibility_flags = ["nodejs_compat"]

# Cloudflare Workers Configuration
workers_dev = true

# Custom Domain for API
[[routes]]
pattern = "api.finhome360.com"
custom_domain = true

# D1 Database (production)
[[d1_databases]]
binding = "DB"
database_name = "finhome-db"
database_id = "1115b8c7-85fd-4ce8-a553-8fe85fb5b629"
migrations_dir = "drizzle/migrations"

# KV Namespaces (production)
[[kv_namespaces]]
binding = "SESSIONS"
id = "17af1f0cba5940188630322248a86071"

[[kv_namespaces]]
binding = "CACHE"
id = "ec9376073fb34ebd9f1dcabbc3cc39ae"

# R2 Buckets
[[r2_buckets]]
binding = "FILES"
bucket_name = "finhome-files"

# Queues
[[queues.producers]]
binding = "BILL_REMINDERS"
queue = "finhome-bill-reminders"

[[queues.consumers]]
queue = "finhome-bill-reminders"
max_batch_size = 10
max_batch_timeout = 30

# Transaction sync queues removed for Bankless edition

# Cron triggers removed (no scheduled banking sync tasks in Bankless edition)

# AI Binding
[ai]
binding = "AI"

# Environment Variables
[vars]
ENVIRONMENT = "production"
JWT_SECRET = "gBk9Lm3Np2Qr5Ts8Wv1Yx4Za7Cd0Ef3Hi6Jl9Mo2Pr5Su8Vx1Za4Cd7Ff0Hi3"
FRONTEND_URL = "https://app.finhome360.com"
RESEND_API_KEY = "re_joNCiaUb_4q5et1qJxxfpNbXPhUbq2pFL"
# TrueLayer variables removed for Bankless edition
# Durable Objects (for future use)
# [[durable_objects.bindings]]
# name = "ANALYTICS"
# class_name = "AnalyticsCounter"
# script_name = "finhome-api"
